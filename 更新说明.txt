# Ver0.7.2.0_PrePub(Build102.2)_SPOL0.6.0
（#Ver0.7.2_Pub(Build102.2)_SPOL0.6.0）

修复了自由文本控制器在设置为右对齐时会崩溃的问题

# Ver0.7.1.0_PrePub(Build102.1005)_SPOL0.6.0
（#Ver0.7.1_Pub(Build102.1005)_SPOL0.6.0）

将交互窗口拖动限定于鼠标拖拽录像带图标的时候，避免全窗口响应拖动导致程序容易被拖拽移位

修复了赞助链接只有拖拽时才能打开，而不是单击打开的问题

修复了窗口在播放提示文字时，窗口会强制回到初始位置的问题。

# Ver0.7.0.0_PrePub(Build101.30)_SPOL0.6.0

修复了大小分支控制器不工作的问题

修复了使用大分支控制器后，后续文件的播放速度直接跳回1.0x的问题

新增了检查更新的延迟时间，最大为5秒

# Ver0.7.0.0_Pre2.1(Build101.19)_SPOL0.6.0

修复了快捷键功能

交互窗口现在可以拖动

进行了优化工作的后续扫尾

# Ver0.7.0.0_Pre2(Build101.10)_SPOL0.6.0

完成了音频播放的初步优化；音频播放还有后续优化过程，多音效下播放性能暂未完整释放

重新加入了自选进度页面，该页面功能未变，但已经经过重构优化，自选进度的功能后续也会更新。

重构期间快捷键均被破坏，暂未修复，请期待后续更新

# Ver0.7.0.0_PreR1.1(Build100.5)_SPOL0.6.0

修复了自由文本控制器引起的崩溃问题

清理了重构后剩下的一些无效代码

# Ver0.7.0.0_PreR1(Build100.2)_SPOL0.6.0

重构了播放器的选择页面、标题页面、播放页面。现在在上述三个页面的性能将有效提升

重构了播放器处理背景变换时唤醒解释器的逻辑，解决了目前已知的最后一个可能会导致解释器偶发卡死从而导致播放器卡死的问题。

重构仍在进行中，用于自选剧情跳转行的页面暂时不加入

重构仍在进行中，可能仍然恶性Bug与优化未尽之处，请期待后续更新

# Ver0.7.0.0_Pre_Tianyi天依(Build87.0)_SPOL0.6.0

现在会在每次启动交互页面时强制检查一次更新

新增了“创作”页面，提供对几个文件夹的快速访问

# Ver0.7.0.0_Pre_XingJi星极(Build85.1)_SPOL0.6.0

加快了动画速度

# Ver0.7.0.0_Pre4(Build85.0)_SPOL0.6.0

新增了自动检查是否可以更新的功能

新增了图形交互页面的“关于”页面

完善了命令行页面“关于”的展示信息

修复了上个版本的部分Bug

# Ver0.7.0.0_Pre3(Build80.1020)_SPOL0.6.0

完善了图形交互页面的部分功能

# Ver0.7.0.0_Pre2(Build76.0):

现在，从剧情播放页退出后会自动回到交互页面而不是命令行页面

在“工具”一栏内加入了清理损坏图像和清除全部缓存的按钮

修改了按钮的着色逻辑

修复了双击打开剧情时，在退出播放页后再进入时仍会尝试直接打开被双击的剧情的问题。

# Ver0.7.0.0_Pre1(Build72.1):

移除了core核心，“spawn”与“line”两个解释模式停用，这两条命令已经被删除。

新增了图形化的顶层交互页面，会在启动程序后自动启动，在图形页面回退到命令行页面后可以使用新增的命令“window”再次启动。

# Ver0.6.0_Pub

激活了系统限制的警示部分，将逐步开启对于老旧系统的限制

更换了符合Windows11风格的程序图标

# Ver0.6.0.0_Pre5

按行自由跳转可以使用滚轮操作

重写了系统判断，系统限制准备启用

HL解释器：新增了对Image关键字的基本识别

SPOL解释器：修复了自由文本控制器的默认值问题

修复了其他Bug

# Ver0.6.0.0_Pre4

修复了上个版本中快捷键的bug

新增按行自由跳转功能，现在用左上角图标或者快捷键“Q”打开此系统

按行跳转页面显示当前解释的行和跳转目标行，拖动右侧进度条来改变跳转目标

*进度条暂时不支持用滚轮移动

新增、修改其他快捷键：

ESC键：在剧情播放时按一次退出到选择页面，选择页面按一次退出UI。新增的按行跳转页面可以用ESC退出

回车键：在剧情播放时如果遇到卡死，可以用回车键尝试唤醒，也可用作手动模式时的翻页快捷键（不推荐）。新增的按行跳转页面可以用回车键确认跳转。

W键：按增加数字轮换播放速度

A键：切换自动、手动模式


# Ver0.6.0.0_Pre3

优化了背景控制器设置极长的淡入时间时的画面帧率，现在帧率不会太低

修复了按下ESC退出UI时，解释器核心还在运行的问题

调整了UI控件结构

为自动按钮新增了动画

剧情播放时的所有按钮现在都支持多语言

在左上角新增了用于调整播放进度的按钮（功能暂未开发，仅有按钮）

所有出字间隔的默认值从0.1改为了0.066（接近原版数值）

HL解释器：优化了识别算法，更改了[Dialog]的识别结果

HL解释器：修复了多人对话的focus值有时不能正确识别的问题

HL解释器：修复了[Blocker]设置为不透明时不能正确反映到SPOL讲述控制器的问题